import { AppRoutes } from '@/routes/AppRoutes';

/**
 * Application shell.
 * Phase 1: routing + foundation providers only.
 * No mock commerce UI.
 */
function App() {
  return <AppRoutes />;
}

export default App;
